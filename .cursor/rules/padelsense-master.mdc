---
description: PadelSense Court System — Mini App архитектура. Читай перед каждым промптом.
alwaysApply: true
---

# PadelSense — Cursor Rules (Mini App)

## 🎯 Суть

PadelSense — умный корт для падел. Игрок открывает **Telegram Mini App** → регистрируется → получает QR → на корте сканирует QR планшетом → выбирает позицию → играет.

## 📁 Твоя зона (Cursor)

```
mini-app/      ← Telegram Mini App (HTML/JS)
backend/       ← FastAPI API
bot/           ← Минимальный бот (запускает Mini App)
shared/        ← Общие типы (читай!)
```

**Windsurf делает:** `tablet-app/`

## 🔗 API Контракт

```
GET  /api/users/by-telegram/{telegram_id}  → User | 404
POST /api/users                            → User (создание)
POST /api/matches                          → Match
```

## 📱 QR Формат

```
user:{telegram_id}
```

Пример: `user:123456789`

## 🏟️ Позиции на корте

```
     СЕТКА
┌─────┬─────┐
│  1  │  2  │ ← Team A
├─────┼─────┤
│  3  │  4  │ ← Team B
└─────┴─────┘
    СТЕКЛО
```

## ⚠️ Важно

1. **Типы** — смотри `shared/types.ts`, Pydantic должен соответствовать
2. **CORS** — backend должен разрешать запросы с Mini App
3. **initData** — Mini App получает user из `Telegram.WebApp.initDataUnsafe.user`

## 📋 Текущие задачи

1. ✅ Mini App UI (есть)
2. ⏳ Backend endpoint `GET /users/by-telegram/{id}`
3. ⏳ Mini App: регистрация через API
4. ⏳ Mini App: проверка юзера при старте

## Стиль кода

- TypeScript: строгие типы, никаких `any`
- Python: type hints, async/await
- Комментарии: русский для бизнес-логики

Полная спецификация: `PADELSENSE_MASTER.md`
Синхронизация: `SYNC.md`
