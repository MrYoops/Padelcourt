# Telegram Bot
BOT_TOKEN=
NOTIFY_PORT=8080
# URL Mini App — публичный HTTPS (не localhost).
# При запуске через "npm start" не задавайте — скрипт подставит URL Cloudflare-туннеля сам.
# НЕ используйте localtunnel (loca.lt) — там страница с паролем, из бота не откроется.
# Для прода: залить mini-app на Vercel/Netlify и указать URL здесь.
# MINI_APP_URL=https://your-app.vercel.app

# Backend -> Bot (for match-end notifications)
BOT_INTERNAL_URL=http://localhost:8080

# Database (local)
DATABASE_URL=postgresql://padelsense:devpass@localhost:5432/padelsense

# Redis
REDIS_URL=redis://localhost:6379

# Supabase (production)
SUPABASE_URL=
SUPABASE_KEY=

# Cloudflare R2
R2_ACCOUNT_ID=
R2_ACCESS_KEY=
R2_SECRET_KEY=
R2_BUCKET=padelsense-videos

# ЮKassa
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET=

# Court
COURT_ID=court-1
COURT_NAME=Корт 1
CLUB_NAME=PadelClub
