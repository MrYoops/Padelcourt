# 🎯 PadelSense — План Настройки Среды Разработки

> Этот документ решает проблему "чиню одно — ломается другое"

---

## 📋 ОГЛАВЛЕНИЕ

1. [Немедленные действия](#1-немедленные-действия-30-минут)
2. [Настройка Cursor](#2-настройка-cursor)
3. [Настройка Windsurf](#3-настройка-windsurf)
4. [Установка расширений](#4-установка-расширений)
5. [Структура проекта](#5-структура-проекта)
6. [Правила работы](#6-правила-работы)
7. [Чеклист синхронизации](#7-чеклист-синхронизации)

---

## 1. НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ (30 минут)

### Шаг 1: Создай единую папку проекта

```bash
# Создаём корневую папку
mkdir padelsense-court
cd padelsense-court

# Создаём структуру
mkdir -p shared/types
mkdir -p telegram-bot/{handlers,keyboards,services,database}
mkdir -p mini-app/src
mkdir -p backend/{api,models,services,workers}
mkdir -p tablet-app/src/{components,features,hooks,services,types,utils}
mkdir -p ml/{training,models,inference}
mkdir -p docs
```

### Шаг 2: Скопируй файлы из этого пакета

Скопируй в корень проекта:
- `.cursorrules`
- `.windsurfrules`
- `shared/types/` (вся папка)

### Шаг 3: Инициализируй Git

```bash
git init
git add .
git commit -m "Initial project structure with AI rules"
```

---

## 2. НАСТРОЙКА CURSOR

### Открой проект в Cursor

```bash
# Открой ВЕСЬ проект (не отдельные папки!)
cursor padelsense-court
```

### Настрой Kimi (опционально, но рекомендую)

1. Открой `Settings > Models`
2. Добавь кастомную модель:
   - Name: `kimi-k2`
   - Base URL: `https://openrouter.ai/api/v1` (или прямой Moonshot API)
   - API Key: твой ключ

### Проверь что .cursorrules читается

1. Открой любой Python файл
2. Нажми `Ctrl+I` (Composer)
3. Напиши: "Какой формат QR-кода в проекте?"
4. AI должен ответить: `user:{telegram_id}`

---

## 3. НАСТРОЙКА WINDSURF

### Открой только tablet-app

```bash
# Windsurf работает ТОЛЬКО с планшетным приложением
windsurf padelsense-court/tablet-app
```

### Скопируй .windsurfrules в tablet-app

```bash
cp padelsense-court/.windsurfrules padelsense-court/tablet-app/
```

### Настрой доступ к shared типам

В `tablet-app/tsconfig.json`:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@shared/*": ["../shared/*"]
    }
  }
}
```

---

## 4. УСТАНОВКА РАСШИРЕНИЙ

### Cursor / VS Code — Обязательные

| Расширение | Зачем |
|------------|-------|
| **Python** (Microsoft) | Базовая поддержка Python |
| **Pylance** | Умный автокомплит + типы |
| **Black Formatter** | Форматирование Python |
| **isort** | Сортировка импортов |
| **Thunder Client** | Тестирование API без Postman |
| **SQLTools** | Работа с базой данных |
| **GitLens** | Удобная работа с Git |
| **Error Lens** | Ошибки прямо в коде |

### Cursor / VS Code — Для Computer Vision (позже)

| Расширение | Зачем |
|------------|-------|
| **Simply View Image for Python Debugging** | Визуализация тензоров |
| **vscode-numpy-viewer** | Просмотр .npy файлов |

### Windsurf — Обязательные

| Расширение | Зачем |
|------------|-------|
| **ESLint** | Линтинг TypeScript |
| **Prettier** | Форматирование |
| **Console Ninja** | Отладка логов без DevTools |
| **Tailwind CSS IntelliSense** | Если используешь Tailwind |
| **React Native Tools** | Отладка RN |

### Оба редактора

| Расширение | Зачем |
|------------|-------|
| **Error Lens** | Ошибки inline |
| **GitLens** | История изменений |
| **Todo Tree** | Поиск TODO/FIXME |

---

## 5. СТРУКТУРА ПРОЕКТА

```
padelsense-court/
│
├── .cursorrules              # Правила для Cursor AI
├── .windsurfrules            # Правила для Windsurf AI
├── .gitignore
├── README.md
│
├── shared/                   # 🔄 ОБЩИЕ ТИПЫ (синхронизировать!)
│   ├── types/
│   │   ├── index.ts
│   │   ├── user.ts
│   │   ├── match.ts
│   │   ├── court.ts
│   │   └── api.ts
│   └── constants/
│
├── telegram-bot/             # 🐍 CURSOR: Python aiogram
│   ├── main.py
│   ├── config.py
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── start.py
│   │   ├── registration.py
│   │   └── qr.py
│   ├── keyboards/
│   ├── services/
│   ├── database/
│   └── requirements.txt
│
├── mini-app/                 # 🌐 CURSOR: React + TypeScript
│   ├── src/
│   │   ├── App.tsx
│   │   ├── pages/
│   │   ├── components/
│   │   └── api/
│   ├── package.json
│   └── vite.config.ts
│
├── backend/                  # 🚀 CURSOR: FastAPI
│   ├── main.py
│   ├── config.py
│   ├── api/
│   │   ├── __init__.py
│   │   ├── users.py
│   │   ├── matches.py
│   │   └── courts.py
│   ├── models/
│   ├── services/
│   ├── workers/
│   └── requirements.txt
│
├── tablet-app/               # 📱 WINDSURF: React Native + Expo
│   ├── .windsurfrules        # Копия правил
│   ├── app/                  # Expo Router
│   ├── src/
│   │   ├── components/
│   │   ├── features/
│   │   ├── hooks/
│   │   ├── services/
│   │   └── types/
│   ├── app.json
│   └── package.json
│
├── ml/                       # 🤖 CURSOR: PyTorch (позже)
│   └── ...
│
└── docs/                     # 📚 Документация
    ├── API.md
    ├── SYNC_LOG.md           # Лог синхронизации
    └── CHANGELOG.md
```

---

## 6. ПРАВИЛА РАБОТЫ

### Правило #1: Один редактор — одна область

```
CURSOR работает с:
├── telegram-bot/
├── mini-app/
├── backend/
└── shared/

WINDSURF работает с:
└── tablet-app/
```

### Правило #2: shared/ — территория согласования

**Перед изменением файла в `shared/`:**

1. Подумай, затронет ли это Windsurf (tablet-app)
2. Если да — сначала опиши изменение в `docs/SYNC_LOG.md`
3. Сделай изменение
4. Уведоми об изменении (или сделай сам в Windsurf)

### Правило #3: API контракты фиксированы

Файл `shared/types/api.ts` — это **контракт**.

**Изменение API = изменение контракта:**
1. Обнови `shared/types/api.ts`
2. Обнови backend endpoint
3. Обнови все клиенты (mini-app, tablet-app)

### Правило #4: Git commit после каждого логического изменения

```bash
# Плохо
git commit -m "Много всего"

# Хорошо
git commit -m "feat(backend): add POST /users endpoint"
git commit -m "feat(shared): add CreateUserRequest type"
git commit -m "feat(tablet): integrate user API"
```

---

## 7. ЧЕКЛИСТ СИНХРОНИЗАЦИИ

### При изменении в Cursor → проверь tablet-app

- [ ] Изменил `shared/types/`? → Скопируй/обнови в tablet-app
- [ ] Изменил API endpoint? → Обнови `tablet-app/src/services/api.ts`
- [ ] Изменил формат данных? → Обнови компоненты в tablet-app

### При изменении в Windsurf → проверь backend

- [ ] Нужен новый endpoint? → Создай задачу для Cursor
- [ ] Изменил формат запроса? → Проверь совместимость с backend

### Еженедельно

- [ ] Проверь `docs/SYNC_LOG.md` на незакрытые изменения
- [ ] Запусти все тесты
- [ ] Обнови зависимости (`npm update`, `pip install -U`)

---

## 🚀 БЫСТРЫЙ СТАРТ

### День 1: Настройка

1. ✅ Создай структуру папок
2. ✅ Скопируй .cursorrules и .windsurfrules
3. ✅ Установи расширения
4. ✅ Инициализируй Git

### День 2-3: Backend + Bot базовый

В **Cursor**:
```
Создай базовый FastAPI сервер с эндпоинтами:
- POST /api/v1/users (создание пользователя)
- GET /api/v1/users/{telegram_id} (получение по TG ID)

Используй типы из shared/types/api.ts
```

### День 4-5: Tablet App скелет

В **Windsurf**:
```
Создай базовую структуру Expo приложения:
- Экран сканирования QR
- Экран выбора позиции
- Заглушка экрана счёта

Используй типы из shared/types/
```

### День 6-7: Интеграция

1. Подключи tablet-app к backend
2. Протестируй флоу: QR → получение юзера → позиция

---

## 💡 ПОДСКАЗКИ ДЛЯ AI

### В Cursor пиши так:

```
Создай хэндлер для команды /start в telegram-bot.
Используй aiogram 3.x, FSM для состояний.
Сохраняй типы согласно shared/types/user.ts
```

### В Windsurf пиши так:

```
Создай компонент QRScanner для сканирования QR-кодов.
Используй expo-camera.
Парсинг QR по формату из shared/types/api.ts (parseQRCodeData)
```

---

*Создано: Январь 2025*
*Версия: 1.0*
